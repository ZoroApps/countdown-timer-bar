{
  "name": "countdown-timer-bar",
  "version": "1.0.0",
  "description": "Backendless Shopify theme app extension for a persistent sales countdown timer bar.",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "dev": "shopify app dev",
    "build": "shopify app build",
    "deploy": "shopify app deploy"
  },
  "workspaces": [
    "extensions/*"
  ],
  "engines": {
    "node": ">=20.10.0"
  }
}
